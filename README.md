# Homebrew Tap for HyperIdle

```bash
brew tap ambrovia/tap
brew install --cask hyperidle
```

## Updating

```bash
brew upgrade --cask hyperidle
```

## Full uninstall (removes database and logs)

```bash
brew uninstall --cask --zap hyperidle
```
